# Environment variables
# Copy this file to .env and adjust as needed:  cp .env.example .env

SECRET_KEY="django-insecure-dev-key-please-change-in-production"
ENV_TYPE="dev"
DEBUG=True
DOMAIN_NAME="http://localhost:8000"
SWAGGER_ROOT_URL="http://localhost:8000"

# AWS (only needed for production / S3 storage)
AWS_BUCKET_NAME=""
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_SES_REGION_NAME=""
AWS_SES_REGION_ENDPOINT=""

# DB
DBNAME="crm_db"
DBUSER="postgres"
DBPASSWORD="postgres"
DBHOST="localhost"
DBPORT="5432"

# Sentry (optional)
SENTRY_DSN=""

# Celery
CELERY_BROKER_URL="redis://localhost:6379/0"
CELERY_RESULT_BACKEND="redis://localhost:6379/0"

# Google OAuth (optional — leave blank to disable)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Email (use "django.core.mail.backends.console.EmailBackend" for dev, "django_ses.SESBackend" for AWS SES)
EMAIL_BACKEND="django.core.mail.backends.console.EmailBackend"
DEFAULT_FROM_EMAIL="noreply@localhost"
ADMIN_EMAIL="admin@localhost"
ADMIN_PASSWORD="admin"

ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173

# Frontend URL (used for links in emails, etc.)
FRONTEND_URL=http://localhost:5173

# TalkHub Omni (optional — leave blank to disable)
TALKHUB_API_URL=""
TALKHUB_PROJECT_TOKEN=""

# MinIO / S3 Storage (only for ENV_TYPE=prod)
AWS_S3_ENDPOINT_URL=""
